/**
 * Product brand — single source of truth for name, kana, and tagline.
 * Use for SEO, PWA manifest, marketing pages, Clerk titles, and in-app copy.
 */

/** English wordmark (lowercase). */
export const PRODUCT_NAME = "mainichi";

/** Japanese reading. */
export const PRODUCT_NAME_KANA = "まいにち";

/** Combined display line (e.g. onboarding hero, about). */
export const PRODUCT_DISPLAY = "mainichi (まいにち)";

/**
 * Primary value proposition — aligned with landing translations `productTagline`
 * (merged into all locales from EN).
 */
export const PRODUCT_TAGLINE = "Capture your story, one day at a time.";
