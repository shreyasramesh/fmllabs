/**
 * Selection feedback chimes are disabled (no audio).
 * Exports remain so existing callers stay valid.
 */
export function playSelectionChime(): void {}

export function playStopChime(): void {}
